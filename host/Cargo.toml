[package]
name = "host"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
methods = { path = "../methods" }
risc0-zkvm = { version = "^3.0.1", default-features = false, features = [
    "prove",
] }
